export default function ConfigRedirectDestinationPage() {
  return (
    <div>
      <h3>next.config.js 리다이렉션 도착 페이지</h3>
      <p>
        <code>/config-redirect-source</code> 에서 성공적으로
        리다이렉션되었습니다.
      </p>
    </div>
  );
}
