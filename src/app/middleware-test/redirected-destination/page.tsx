export default function RedirectedDestinationPage() {
  return (
    <div>
      <h2>리다이렉션 도착 페이지</h2>
      <p>
        성공적으로 <code>/middleware-test/redirected-destination</code>으로
        리다이렉션되었습니다.
      </p>
      <p>이것은 Middleware에 의해 처리되었습니다.</p>
    </div>
  );
}
